// Compile with "std=c++2a" flag
#include <iostream>
#include <algorithm>
#include <queue>
#include <vector>

int main() {
    int k, n;
    std::cin >> k >> n;
    std::vector<int> sosu(k, 0);
    for(int i=0; i<k; i++) std::cin >> sosu[i];
}