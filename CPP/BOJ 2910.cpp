#include <iostream>
#include <algorithm>
#include <tuple>
#include <unordered_map>

int main() {
    int n, c;
    std::cin >> n >> c;
    std::unordered_map<int, int> map;
    for(int i=0; i<n; i++) {
        int num;
        std::cin >> num;
        map[num]
    }
}